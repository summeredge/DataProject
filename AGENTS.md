# DataProject Agent 工作规则

## 项目定位

DataProject 用于从工业历史时序数据中筛选与目标变量存在较强、稳定、可信统计关联，值得工艺工程师进一步复核的候选变量。

本项目不自动：

* 证明确定性因果；
* 确认根因；
* 识别控制变量；
* 生成控制策略。

## 核心规则

1. 第一阶段主筛查必须独立于后续验证结果。

2. `ranked_features.csv` 初筛排序必须以 `final_score` 为准。

3. Granger、条件 Granger、模型解释、综合复核、XGBoost 不得改变：

* `final_score`
* 初筛 Top-K
* 初筛推荐顺序

4. 闭环状态和人工闭环标签不得影响初筛评分和排序。

5. 未执行的后续分析不得在初筛页面、API 或报告中显示为已有结论。

6. 缺失、未执行、不可计算和真实 `0.0` 必须保持不同语义。

7. 不得使用 `abs(lag)`、`abs(best_lag)` 等方式丢失滞后方向。

8. 统计相关、预测增量和 Granger 结果不得表述为确定性因果。

9. 新增字段不得自动进入评分、排序或默认流程。

10. 未经明确要求，不得扩大产品范围或修改核心算法。

## 文档读取规则

默认只读取必要文档，不加载全部 docs。

任务相关文档：

* 产品目标、算法边界：
  `docs/product.md`

* 系统结构、阶段流转：
  `docs/architecture.md`

* 评分、排序、CSV、API：
  `docs/contracts.md`

* 测试和回归：
  `docs/testing.md`

* Review、多 Agent 流程：
  `docs/workflow.md`

* 术语定义：
  `docs/glossary.md`

* 功能范围：
  `docs/roadmap.md`

涉及历史设计或恢复旧逻辑：

1. 先查看 `docs/decisions/README.md`
2. 再读取相关 ADR

## 修改原则

修改前：

* 阅读任务和验收标准；
* 检查相关代码和测试；
* 检查 `git status` 和 `git diff`；
* 保护已有修改。

修改时：

* 优先最小修改；
* 不重构无关代码；
* 不修改无关接口、字段和文件；
* 不引入未要求的新算法或依赖；
* 不自动提交、推送或合并。

同类逻辑存在多个位置时，必须检查并修复所有实现。

## 契约和兼容

必须保护：

* CSV 文件名、字段名、字段语义；
* API 输出结构；
* 中文 UI 字段；
* Windows 路径和中文路径；
* Excel/CSV 读取兼容。

新增行为必须通过测试约束，不得只依赖文档。

## 测试原则

修改后必须：

* 执行相关单元测试；
* 执行契约测试；
* 根据范围执行完整测试。

测试应优先验证：

1. 用户可见行为；
2. 数据流和接口契约；
3. 必要的源码约束。

不得：

* 删除测试；
* 放宽断言绕过问题；
* 仅修改预期结果；
* 用 CI 通过替代 Review。

## Review 原则

Review 必须检查：

* 完整 diff；
* 修改范围；
* 产品目标；
* 架构边界；
* 数据契约；
* 测试覆盖。

重点关注：

* 后续分析是否影响初筛；
* 是否修改 `final_score` 语义；
* 是否遗漏重复实现；
* 是否破坏兼容性；
* 是否引入无关修改。

CI 通过不等于 Review 通过。

# Agent 工作规则

## 角色

主 Agent 负责：
- 理解需求；
- 判断任务模式；
- 调度子 Agent；
- 验收结果。

`luna_worker` 负责：
- 修改代码；
- 执行测试；
- 返回结果。

## Delegation

当任务需要修改源代码时，必须使用 multi_agent 工具调用 luna_worker,主agent不负责修改代码。

---

## 模式 1：已有明确任务

如果用户已提供完整 Codex 指令或明确修改要求：

主 Agent 仅作为 verifier / dispatcher。

执行：
1. 检查任务与当前代码是否冲突；
2. 补充必要仓库信息；
3. 调用 `luna_worker`。

禁止：
- 重新规划；
- 改写任务；
- 重复生成步骤；
- 扩大范围。

---

## 模式 2：普通需求

如果用户只提供目标：

主 Agent 先生成简要规划：

- 目标
- 涉及模块
- 行为变化
- 关键实现约束
- 验收标准

然后调用 `luna_worker`。

---

## 子 Agent 调用

代码修改任务必须优先调用：
luna_worker

传递：
- 目标；
- 模块；
- 行为变化；
- 约束；
- 验收标准。

不要传递：
- 历史讨论；
- 重复分析；
- 无关背景。

---

## 实现原则

`luna_worker`：
- 修改前读取代码；
- 最小化修改；
- 保持接口和数据结构；
- 不做无关重构；
- 不增加未要求功能。

---

## 验收

完成后主 Agent 检查：
- 目标是否满足；
- 约束是否遵守；
- 测试是否通过。

发现问题时，仅创建修复任务再次调用 `luna_worker`。

---

## Token 原则

已有任务，只校验；
没有任务，才规划。

主 Agent 决定：
> 做什么、边界是什么。

`luna_worker` 决定：
> 如何实现。

